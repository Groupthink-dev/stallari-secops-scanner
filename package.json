{
  "name": "stallari-secops-scanner",
  "version": "0.3.0",
  "description": "Security scanner for Stallari packs — prompt injection, clone detection, and threat matching",
  "type": "module",
  "bin": {
    "stallari-secops-scanner": "./dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "stallari",
    "security",
    "prompt-injection",
    "scanner"
  ],
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2"
  }
}
